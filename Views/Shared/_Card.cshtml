<div class="col-sm-4 col-md-4 col-lg-4">
    <p><img src="@Model.ImagemUrl" class="img-fluid" asp-append-version="true"></p>
    <h3>Preço : @Model.Valor.ToString("c")</h3>
    <h4>
    <a asp-controller="Item" asp-action="Details" asp-route-ItemId="@Model.MovelId">@Model.Nome</a>
    </h4>
    <p>@Model.DscricaoCurta</p>
    <div>
        <p class="button">
            <a class="btn btn-primary" id="cartButton" asp-controller="Carrinho"
                asp-action="AdicionarItemNoCarrinhoCompra" asp-route-movelId="@Model.ItemId">
                Incluir no Carrinho
            </a>
        </p>
    </div>
</div>